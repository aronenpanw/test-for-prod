name: Deploy Docker Image

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Simulate deployment of image
        run: |
          echo "Deploying image ghcr.io/${{ github.repository }}:latest"
          echo "Deployment completed"
